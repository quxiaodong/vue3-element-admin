<template>
  <el-form-item v-if="locales" label="当前语言">
    <el-select :model-value="locale?.code" @change="toggleLocale">
      <el-option
        v-for="{ code, name, icon } in locales"
        :key="code"
        :value="code"
      >
        {{ icon }} {{ name }}
      </el-option>
      <template #label>{{ locale?.icon }} {{ locale?.name }}</template>
    </el-select>
  </el-form-item>
</template>

<script setup lang="ts">
import {
  locale,
  locales,
  toggleLocale
} from '@/components/common-locale/stores'
</script>
